!function numberGuesser(){let min=1,max=10,guessesLeft=3,winningNum=getWinningNum(max,1);const minNum=document.querySelector(".min-num"),maxNum=document.querySelector(".max-num"),guessInput=document.querySelector("#guess-input"),guessBtn=document.querySelector("#guess-btn"),message=document.querySelector(".message");function guessFunc(e){let guess=parseInt(guessInput.value);isNaN(guess)||guess>max||guess<minNum?console.log("hekhek"):guess===winningNum?gameOver(!0,`You guessed ${guess}. YOU WIN`):0!==--guessesLeft?setMessage(`${guess} is incorrect. You only have ${guessesLeft} guesses left`,"red"):gameOver(!1,"Game Over. YOU LOSE")}function setMessage(msg,color){message.textContent=msg,message.style.color=color,guessInput.style.borderColor=color}function gameOver(gameStatus,msg){color=!0===gameStatus?"green":"red",guessInput.disabled=!0,guessInput.style.borderColor=color,guessBtn.value="Play Again",guessBtn.className+="play-again",setMessage(msg,color)}function getWinningNum(max,min){return Math.floor(Math.random()*(max-min+1)+min)}game=document.querySelector("#game"),minNum.textContent=1,maxNum.textContent=max,guessBtn.addEventListener("click",guessFunc),game.addEventListener("mousedown",(function(e){"play-again"===e.target.className&&window.location.reload()}))}();